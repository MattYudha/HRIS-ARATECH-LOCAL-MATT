<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HRIS Aratech</title>
    <meta name="csrf-token" content="{{ csrf_token() }}">

    <link rel="shortcut icon" href="{{ asset('img/HRIS ARATECH logo tr.png') }}" type="image/png">

    <!-- Mazer CSS -->
    <link rel="stylesheet" href="{{ asset('mazer/assets/compiled/css/app.css') }}">
    <link rel="stylesheet" href="{{ asset('mazer/assets/compiled/css/app-dark.css') }}">
    
    <!-- DataTables CSS -->
    <link rel="stylesheet" href="{{ asset('vendor/datatables/dataTables.bootstrap5.min.css') }}">
    <link rel="stylesheet" href="{{ asset('vendor/datatables/responsive.bootstrap5.min.css') }}">
    
    <link rel="stylesheet" href="{{ asset('vendor/flatpickr/flatpickr.min.css') }}">

    <!-- CUSTOM -->
    <style>
        /* Dropdown tanpa animasi */
        .sidebar-item.has-sub .submenu {
            display: none;
            padding-left: 1.5rem;
        }
        .sidebar-item.has-sub.active .submenu {
            display: block;
        }
    </style>
</head>

<body>
<script src="{{ asset('mazer/assets/static/js/initTheme.js') }}"></script>

<div id="app">

    <!-- ================= SIDEBAR ================= -->
    <div id="sidebar">
        <div class="sidebar-wrapper active">

            <div class="sidebar-header text-center">
                <a href="{{ url('/dashboard') }}">
                    <img src="{{ asset('img/HRIS ARATECH logo tr.png') }}" style="height:160px">
                </a>
            </div>

            <div class="sidebar-menu">
                <ul class="menu">

                    @php
                        $role = session('role');
                        $isPowerUser = $role === 'Power User';
                        $isHR = $role === 'HR';
                        $isManager = $role === 'Manager';
                        $isDeveloper = $role === 'Developer';
                        $isSales = $role === 'Sales';
                        $isEmployee = $role === 'Common Employee';

                        $isAdmin = $isPowerUser || $isHR;
                        $isDevOrAdmin = $isAdmin || $isDeveloper;
                        $isManagerOrAdmin = $isAdmin || $isManager;
                        $isStaff = $isDevOrAdmin || $isManager || $isSales || $isEmployee;
                    @endphp

                    <!-- DASHBOARD -->
                    <li class="sidebar-item {{ request()->is('knowledge-base*') ? 'active' : '' }}">
                        <a href="{{ url('/dashboard') }}" class="sidebar-link">
                            <i class="bi bi-grid-fill"></i>
                            <span>Dashboard</span>
                        </a>
                    </li>

                    <!-- HR MANAGEMENT -->
                    <li class="sidebar-item has-sub">
                        <a href="#" class="sidebar-link">
                            <i class="bi bi-people-fill"></i>
                            <span>HR Management</span>
                        </a>
                        <ul class="submenu">
                            @if($isAdmin || $isManager || $isDeveloper)
                            <li class="submenu-item"><a href="/employees">Employees</a></li>
                            @endif
                            @if($isAdmin)
                            <li class="submenu-item"><a href="{{ route('employee-approvals.index') }}">Update Approvals</a></li>
                            <li class="submenu-item"><a href="/departments">Departments</a></li>
                            <li class="submenu-item"><a href="/roles">Roles</a></li>
                            @endif
                            <li class="submenu-item"><a href="/tasks">Tasks</a></li>
                            <li class="submenu-item"><a href="/leave-requests">Leave Requests</a></li>
                        </ul>
                    </li>


                    <!-- PAYROLL & KPI -->
                    <li class="sidebar-item has-sub">
                        <a href="#" class="sidebar-link">
                            <i class="bi bi-currency-dollar"></i>
                            <span>Payroll & KPI</span>
                        </a>
                        <ul class="submenu">
                            @if($isDevOrAdmin)
                            <li class="submenu-item"><a href="/payrolls">Payrolls</a></li>
                            @endif
                            <li class="submenu-item"><a href="/kpi/dashboard">KPI Dashboard</a></li>
                            
                            @if($isManagerOrAdmin)
                            <li class="submenu-item"><a href="/kpi/team">Team KPI</a></li>
                            <li class="submenu-item"><a href="/kpi/department">Department KPI</a></li>
                            <li class="submenu-item"><a href="{{ route('kpi.pending') }}">Pending Approvals</a></li>
                            @endif
                        </ul>
                    </li>

                    <!-- INVENTORY -->
                    <li class="sidebar-item has-sub">
                        <a href="#" class="sidebar-link">
                            <i class="bi bi-boxes"></i>
                            <span>Inventory</span>
                        </a>
                        <ul class="submenu">
                            @if($isAdmin)
                            <li class="submenu-item"><a href="/inventory-categories">Categories</a></li>
                            <li class="submenu-item"><a href="/inventories">Inventories</a></li>
                            @endif
                            <li class="submenu-item"><a href="/inventory-usage-logs">Usage Logs</a></li>
                            <li class="submenu-item"><a href="/inventory-requests">Requests</a></li>
                        </ul>
                    </li>

                    <!-- LETTERS -->
                    <li class="sidebar-item has-sub">
                        <a href="#" class="sidebar-link">
                            <i class="bi bi-envelope-fill"></i>
                            <span>Letters</span>
                        </a>
                        <ul class="submenu">
                            <li class="submenu-item"><a href="/letters">Letters</a></li>
                            @if($isAdmin)
                            <li class="submenu-item"><a href="/letter-templates">Templates</a></li>
                            <li class="submenu-item"><a href="/letter-configurations">Configurations</a></li>
                            @endif
                            @if($isDevOrAdmin)
                            <li class="submenu-item"><a href="/letter-archives">Archives</a></li>
                            @endif
                            <li class="submenu-item"><a href="/signature-logs">Signature Logs</a></li>
                        </ul>
                    </li>

                    <!-- REPORTS -->
                    @if($isManagerOrAdmin)
                    <li class="sidebar-item has-sub">
                        <a href="#" class="sidebar-link">
                            <i class="bi bi-file-earmark-text"></i>
                            <span>Reports</span>
                        </a>
                        <ul class="submenu">
                            @if($isAdmin)
                            <li class="submenu-item"><a href="/reports/executive">Executive Report</a></li>
                            @endif
                            <li class="submenu-item"><a href="/reports/monthly-recap">Monthly Report</a></li>
                        </ul>
                    </li>
                    @endif

                    <!-- PERSONAL -->
                    <li class="sidebar-title">Personal & Theme</li>


                    <li class="sidebar-item {{ request()->is('my-profile') ? 'active' : '' }}">
                        <a href="{{ route('my-profile') }}" class="sidebar-link">
                            <i class="bi bi-person-fill"></i>
                            <span>My Profile</span>
                        </a>
                    </li>

                    <li class="sidebar-item {{ request()->is('presences') ? 'active' : '' }}">
                        <a href="/presences" class="sidebar-link">
                            <i class="bi bi-table"></i>
                            <span>Presences</span>
                        </a>
                    </li>

                    <li class="sidebar-item {{ request()->is('knowledge-base*') ? 'active' : '' }}">
                        <a href="/knowledge-base" class="sidebar-link">
                            <i class="bi bi-book"></i>
                            <span>Knowledge Base</span>
                        </a>
                    </li>

                    <li class="sidebar-item {{ request()->is('knowledge-base*') ? 'active' : '' }}">
                        <a href="/logout" class="sidebar-link">
                            <i class="bi bi-box-arrow-right"></i>
                            <span>Logout</span>
                        </a>
                    </li>

                </ul>
            </div>
        </div>
    </div>
    <!-- ================= END SIDEBAR ================= -->

    <!-- ================= MAIN ================= -->
    <div id="main">
       
        @yield('content')

        <footer class="footer clearfix mb-0 text-muted">
            <div class="float-start">
                <p>2025 &copy; PT Aratech Nusantara Indonesia</p>
            </div>
            <div class="float-end">
                <p>Crafted by <a href="https://aratechnology.id">Aratech</a></p>
            </div>
        </footer>
    </div>

</div>

<!-- ================= JS ================= -->
<script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
<script src="{{ asset('vendor/datatables/jquery.dataTables.min.js') }}"></script>
<script src="{{ asset('vendor/datatables/dataTables.bootstrap5.min.js') }}"></script>
<script src="{{ asset('vendor/datatables/dataTables.responsive.min.js') }}"></script>
<script src="{{ asset('vendor/datatables/responsive.bootstrap5.min.js') }}"></script>
<script src="{{ asset('mazer/assets/compiled/js/app.js') }}"></script>
<script src="{{ asset('vendor/flatpickr/flatpickr.min.js') }}"></script>

    <script>
        // Dropdown Mazer (tanpa animasi, tanpa icon dobel)
        document.querySelectorAll('.sidebar-item.has-sub > .sidebar-link')
            .forEach(link => {
                link.addEventListener('click', e => {
                    e.preventDefault();
                    link.parentElement.classList.toggle('active');
                });
            });

        flatpickr(".date", { dateFormat: "Y-m-d" });

        // Theme Toggle Logic
        const themeToggle = document.getElementById('theme-toggle');
        const themeToggleIcon = document.getElementById('theme-toggle-icon');

        function updateIcon(theme) {
            if (theme === 'dark') {
                themeToggleIcon.className = 'bi bi-moon-fill';
            } else {
                themeToggleIcon.className = 'bi bi-sun-fill';
            }
        }

        // Sync icon on load
        updateIcon(localStorage.getItem('theme') || 'light');

        themeToggle.addEventListener('click', (e) => {
            e.preventDefault();
            const currentTheme = localStorage.getItem('theme') || 'light';
            const newTheme = currentTheme === 'dark' ? 'light' : 'dark';
            
            localStorage.setItem('theme', newTheme);
            
            if (newTheme === 'dark') {
                document.documentElement.setAttribute('data-bs-theme', 'dark');
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.setAttribute('data-bs-theme', 'light');
                document.documentElement.classList.remove('dark');
            }
            
            updateIcon(newTheme);
        });
    </script>

    @stack('scripts')

</body>
</html>
